{
  "name": "shadcn-ui-monorepo",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "docker:dev": "docker-compose up --build -d",
    "prepare": "husky install",
    "prisma:generate": "pnpm --filter server exec prisma generate",
    "prisma:seed": "pnpm --filter server exec ts-node prisma/seed/seed.ts",
    "prisma:migrate": "pnpm --filter server exec prisma migrate dev",
    "prisma:migrate:deploy": "pnpm --filter server exec prisma migrate deploy",
    "prisma:migrate:reset": "pnpm --filter server exec prisma migrate reset",
    "prisma:migrate:dev": "pnpm --filter server exec prisma migrate dev"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.3.1",
    "@commitlint/config-conventional": "^20.3.1",
    "@workspace/eslint-config": "workspace:*",
    "@workspace/typescript-config": "workspace:*",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.6.2",
    "turbo": "^2.5.5",
    "typescript": "5.7.3"
  },
  "packageManager": "pnpm@10.4.1",
  "engines": {
    "node": ">=20"
  }
}
